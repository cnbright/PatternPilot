# Release Notes

## Current Native Release

- Renamed the native application to `PatternPilot`
- Switched the executable branding, application title, and startup metrics path to `PatternPilot`
- Updated the home screen to a detailed Chinese usage guide
- Replaced the application icon with the selected detective-cat icon
- Changed `One Third` rendering to `1/3` white and `2/3` black
- Added GitHub-ready documentation and screenshot pages
- Kept the native build and publish scripts for both directory and single-file releases

## Distribution

Directory publish output:

- `PatternPilot/artifacts/dir/PatternPilot.exe`

Single-file publish output:

- `PatternPilot/artifacts/single/PatternPilot.exe`
